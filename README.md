# HTML Jenkins Test Project

This is a simple HTML/CSS/JS project used to test Jenkins CI/CD pipeline with Docker.

## Pipeline Stages
- Checkout code
- Build Docker image
- Run container on port 8080
